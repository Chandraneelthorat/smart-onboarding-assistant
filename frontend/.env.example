# Leave empty to use Vite dev proxy to the API (same origin /api -> backend).
# Or set full API URL, e.g. http://127.0.0.1:8000
# VITE_API_BASE_URL=

# Must match server API_KEY (or HR_API_KEY) when that env is set — same key for HR and Employee UIs
# VITE_API_KEY=
# VITE_HR_API_KEY=
